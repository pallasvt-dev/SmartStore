@model SmartStore.Models.ProductFormViewModel
@{
    ViewData["Title"] = "Thêm sản phẩm";
}

<section class="admin-section">
    <div class="container narrow-container">
        <div class="admin-page-heading">
            <a class="back-link" asp-action="Index"><i class="fa-solid fa-arrow-left"></i> Quay lại danh sách</a>
            <span class="eyebrow dark"><i class="fa-solid fa-plus"></i> Thêm mới</span>
            <h1>Thêm sản phẩm mới</h1>
        </div>

        <form asp-action="Create" method="post" class="admin-product-form">
            @Html.AntiForgeryToken()
            <partial name="_ProductForm" model="Model" />
            <div class="form-actions">
                <button type="submit" class="btn btn-primary-custom"><i class="fa-solid fa-floppy-disk me-1"></i> Lưu sản phẩm</button>
                <a class="btn btn-outline-custom" asp-action="Index">Quay lại danh sách</a>
            </div>
        </form>
    </div>
</section>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
